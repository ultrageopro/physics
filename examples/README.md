# Examples

These modules are required to run the examples

```toml
[project]
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.7",
    "tqdm>=4.67.1",
]
```

also install the `physics` wheel
